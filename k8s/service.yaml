apiVersion: v1
kind: Service
metadata:
  name: appscrip-service
  namespace: default
  labels:
    app: appscrip
spec:
  type: ClusterIP
  selector:
    app: appscrip
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
